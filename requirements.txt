# QSR-ETA Phase 2 — all pins verified to exist on PyPI and resolve together
# (pip dry-run clean). Python 3.11.
#
# Runtime (serving image) + test deps. Test deps are included here because CI
# installs from this file to run pytest; they add little to the image.

# --- runtime ---
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
xgboost==2.1.4
statsmodels==0.14.4
scipy==1.13.1
shap==0.46.0
boto3==1.35.76
joblib==1.4.2
fastapi==0.115.6
pydantic==2.10.4
uvicorn==0.34.0

# --- test (needed by tests/test_model.py: pytest, moto for mocked S3,
#     httpx for fastapi TestClient) ---
pytest==8.3.4
moto[s3]==5.0.28
httpx==0.28.1
